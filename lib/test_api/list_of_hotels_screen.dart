import 'package:flutter/material.dart';

class ListOfHotels extends StatelessWidget {
  const ListOfHotels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SizedBox(
        
      ),
    );
  }
}